name: Deploy to VPS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: vps-5600697-x.dattaweb.com
          username: root
          port: 5371
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            cd /root/deploy/PayGlobal
            git pull origin main
            docker compose down
            docker compose build --no-cache
            docker compose up -d